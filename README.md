# Create symlink
-------------------------------------------------------------------------------
To create a symlink between the tmux config folder and the home dir:
`ln -s ~/.config/tmux/.tmux.conf ~/.tmux.conf`
